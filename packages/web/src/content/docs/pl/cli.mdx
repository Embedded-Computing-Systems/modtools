---
title: CLI
description: Opcje i polecenia wiersza poleceń Model-of-Design Tools.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI Model-of-Design Tools domyślnie uruchamia [TUI](/docs/tui), gdy wywołasz je bez argumentów.

```bash
modtools
```

CLI obsługuje też polecenia opisane na tej stronie, więc możesz używać Model-of-Design Tools programowo.

```bash
modtools run "Explain how closures work in JavaScript"
```

---

### tui

Uruchamia interfejs terminalowy (TUI) Model-of-Design Tools.

```bash
modtools [project]
```

#### Flagi

| Flaga        | Skrót | Opis                                                                    |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--continue` | `-c`  | Kontynuuj ostatnią sesję                                                |
| `--session`  | `-s`  | Identyfikator sesji do kontynuowania                                    |
| `--fork`     |       | Sklonuj sesję podczas kontynuacji (użyj z `--continue` lub `--session`) |
| `--prompt`   |       | Monit do użycia                                                         |
| `--model`    | `-m`  | Model do użycia w formacie dostawca/model                               |
| `--agent`    |       | Agent do użycia                                                         |
| `--port`     |       | Port do nasłuchiwania                                                   |
| `--hostname` |       | Nazwa hosta, do której należy się powiązać                              |

---

## Polecenia

Interfejs CLI Model-of-Design Tools zawiera także dodatkowe polecenia.

---

### agent

Zarządzaj agentami Model-of-Design Tools.

```bash
modtools agent [command]
```

---

### attach

Dołącz terminal do już działającego serwera backendu Model-of-Design Tools uruchomionego za pomocą `serve` lub `web`.

```bash
modtools attach [url]
```

Pozwala to połączyć TUI ze zdalnym backendem Model-of-Design Tools. Na przykład:

```bash
# Start the backend server for web/mobile access
modtools web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
modtools attach http://10.20.30.40:4096
```

#### Flagi

| Flaga       | Skrót | Opis                                    |
| ----------- | ----- | --------------------------------------- |
| `--dir`     |       | Katalog roboczy, w którym uruchomić TUI |
| `--session` | `-s`  | Identyfikator sesji do kontynuowania    |

---

#### create

Utwórz nowego agenta z niestandardową konfiguracją.

```bash
modtools agent create
```

To polecenie poprowadzi Cię przez proces tworzenia nowego agenta z niestandardowym promptem systemowym i konfiguracją narzędzi.

---

#### list

Wyświetl listę wszystkich agentów.

```bash
modtools agent list
```

---

### auth

Zarządzaj danymi uwierzytelniającymi i logowaniem dla dostawców.

```bash
modtools auth [command]
```

---

#### login

Model-of-Design Tools integruje się z wieloma dostawcami. Możesz użyć `modtools auth login`, aby skonfigurować klucz API dla dowolnego obsługiwanego dostawcy. Klucze są bezpiecznie przechowywane w `~/.local/share/modtools/auth.json`.

```bash
modtools auth login
```

Podczas uruchamiania Model-of-Design Tools ładuje dostawców z pliku poświadczeń, a także ze zmiennych środowiskowych lub pliku `.env` w Twoim projekcie.

---

#### list

Wyświetl listę wszystkich uwierzytelnionych dostawców przechowywanych w pliku poświadczeń.

```bash
modtools auth list
```

Lub skrócona wersja:

```bash
modtools auth ls
```

---

#### logout

Wyloguj się z dostawcy, usuwając go z pliku poświadczeń.

```bash
modtools auth logout
```

---

### github

Zarządzaj agentem GitHub w celu automatyzacji repozytorium.

```bash
modtools github [command]
```

---

#### install

Zainstaluj agenta GitHub w swoim repozytorium.

```bash
modtools github install
```

Spowoduje to utworzenie pliku workflow GitHub Actions i poprowadzi Cię przez proces konfiguracji. [Dowiedz się więcej](/docs/github).

---

#### run

Uruchom agenta na GitHubie. Zwykle używane w GitHub Actions.

```bash
modtools github run
```

##### Flagi

| Flaga     | Opis                                     |
| --------- | ---------------------------------------- |
| `--event` | Zdarzenie GitHub, które wyzwoliło agenta |
| `--token` | Osobisty token dostępu GitHub            |

---

### mcp

Zarządzaj serwerami Model Context Protocol (MCP).

```bash
modtools mcp [command]
```

---

#### add

Dodaj serwer MCP do swojej konfiguracji.

```bash
modtools mcp add
```

To polecenie poprowadzi Cię przez proces dodawania lokalnego lub zdalnego serwera MCP.

---

#### list

Wyświetl listę wszystkich skonfigurowanych serwerów MCP i ich status.

```bash
modtools mcp list
```

Lub skrócona wersja:

```bash
modtools mcp ls
```

---

#### auth

Uwierzytelnij się w serwerze MCP z włączoną funkcją OAuth.

```bash
modtools mcp auth [name]
```

Jeśli nie podasz nazwy serwera, zostaniesz poproszony o wybranie jednego z serwerów wymagających uwierzytelnienia.

Możesz także wyświetlić listę serwerów obsługujących OAuth i ich status uwierzytelnienia.

```bash
modtools mcp auth list
```

Lub skrócona wersja:

```bash
modtools mcp auth ls
```

---

#### logout

Usuń tokeny OAuth dla serwera MCP.

```bash
modtools mcp logout [name]
```

---

#### debug

Debuguj problemy z połączeniem OAuth dla serwera MCP.

```bash
modtools mcp debug <name>
```

---

### models

Wyświetl listę modeli ze wszystkich skonfigurowanych dostawców.

```bash
modtools models [provider]
```

Pokazuje modele dostępne do użycia w `provider/model`.

Jest to identyfikator modelu, którego używasz w [konfiguracji](/docs/config).

Opcjonalnie możesz podać identyfikator dostawcy, aby filtrować modele według tego dostawcy.

```bash
modtools models anthropic
```

#### Flagi

| Flaga       | Opis                                                                            |
| ----------- | ------------------------------------------------------------------------------- |
| `--refresh` | Odśwież pamięć podręczną modeli                                                 |
| `--verbose` | Bardziej szczegółowe dane wyjściowe modelu (zawiera metadane, takie jak koszty) |

Użyj flagi `--refresh`, aby zaktualizować listę modeli w pamięci podręcznej. Jest to przydatne, gdy dostawca dodał nowe modele, które chcesz zobaczyć w Model-of-Design Tools.

```bash
modtools models --refresh
```

---

### run

Uruchom Model-of-Design Tools w trybie nieinteraktywnym, przekazując bezpośrednio prompt.

```bash
modtools run [message..]
```

Jest to przydatne do tworzenia skryptów, automatyzacji lub gdy chcesz wysłać szybkie zapytanie bez uruchamiania TUI. Na przykład:

```bash "modtools run"
modtools run Explain the use of context in Go
```

Można również użyć tego z działającym `modtools serve`, aby uniknąć zimnego startu serwera MCP przy każdym uruchomieniu:

```bash
# Start a headless server in one terminal
modtools serve

# In another terminal, run commands that attach to it
modtools run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagi

| Flaga        | Skrót | Opis                                                                    |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--command`  |       | Polecenie do uruchomienia, reszta to argumenty                          |
| `--continue` | `-c`  | Kontynuuj ostatnią sesję                                                |
| `--session`  | `-s`  | Identyfikator sesji do kontynuowania                                    |
| `--fork`     |       | Sklonuj sesję podczas kontynuacji (użyj z `--continue` lub `--session`) |
| `--share`    |       | Udostępnij sesję po zakończeniu                                         |
| `--model`    | `-m`  | Model do użycia w formacie dostawca/model                               |
| `--agent`    |       | Agent do użycia                                                         |
| `--file`     | `-f`  | Pliki do załączenia do wiadomości                                       |
| `--format`   |       | Format wyjściowy: `default` (sformatowany) lub `json` (surowy JSON)     |
| `--title`    |       | Tytuł sesji (jeśli nie podano, zostanie wygenerowany z promptu)         |
| `--attach`   |       | Dołącz do działającego serwera Model-of-Design Tools (np. http://localhost:4096)     |
| `--port`     |       | Port dla serwera lokalnego (domyślnie losowy)                           |

---

### serve

Uruchom serwer Model-of-Design Tools (bez interfejsu) w celu uzyskania dostępu do API. Pełny opis API HTTP znajduje się w [dokumentacji serwera](/docs/server).

```bash
modtools serve
```

Uruchamia to serwer HTTP, który zapewnia dostęp do API Model-of-Design Tools bez interfejsu TUI. Ustaw `MOD_SERVER_PASSWORD`, aby włączyć podstawowe uwierzytelnianie HTTP (domyślna nazwa użytkownika to `modtools`).

#### Flagi

| Flaga        | Opis                                       |
| ------------ | ------------------------------------------ |
| `--port`     | Port do nasłuchiwania                      |
| `--hostname` | Nazwa hosta, do której należy się powiązać |
| `--mdns`     | Włącz wykrywanie mDNS                      |
| `--cors`     | Dodatkowe dozwolone źródła CORS            |

---

### session

Zarządzaj sesjami Model-of-Design Tools.

```bash
modtools session [command]
```

---

#### list

Lista wszystkich sesji Model-of-Design Tools.

```bash
modtools session list
```

##### Flagi

| Flaga         | Skrót | Opis                                       |
| ------------- | ----- | ------------------------------------------ |
| `--max-count` | `-n`  | Ogranicz do ostatnich N sesji              |
| `--format`    |       | Format wyjściowy: tabela lub json (tabela) |

---

### stats

Pokaż statystyki wykorzystania tokenów i sesji Model-of-Design Tools.

```bash
modtools stats
```

#### Flagi

| Flaga       | Opis                                                                                  |
| ----------- | ------------------------------------------------------------------------------------- |
| `--days`    | Pokaż statystyki z ostatnich N dni (domyślnie: cały czas)                             |
| `--tools`   | Pokaż użycie poszczególnych narzędzi (domyślnie: wszystkie)                           |
| `--models`  | Pokaż podział na modele (domyślnie ukryty). Podaj liczbę, aby pokazać N najczęstszych |
| `--project` | Filtruj według projektu (domyślnie: wszystkie projekty, pusty ciąg: bieżący projekt)  |

---

### export

Eksportuj dane sesji jako JSON.

```bash
modtools export [sessionID]
```

Jeśli nie podasz identyfikatora sesji, zostaniesz poproszony o wybranie jednej z ostatnich sesji.

---

### import

Importuj dane sesji z pliku JSON lub adresu URL udostępniania Model-of-Design Tools.

```bash
modtools import <file>
```

Możesz importować z pliku lokalnego lub adresu URL.

```bash
modtools import session.json
modtools import https://opncd.ai/s/abc123
```

---

### web

Uruchom serwer Model-of-Design Tools z interfejsem internetowym.

```bash
modtools web
```

Uruchamia to serwer HTTP i udostępnia Model-of-Design Tools przez interfejs przeglądarkowy. Ustaw `MOD_SERVER_PASSWORD`, aby włączyć podstawowe uwierzytelnianie HTTP (domyślna nazwa użytkownika to `modtools`).

#### Flagi

| Flaga        | Opis                                       |
| ------------ | ------------------------------------------ |
| `--port`     | Port do nasłuchiwania                      |
| `--hostname` | Nazwa hosta, do której należy się powiązać |
| `--mdns`     | Włącz wykrywanie mDNS                      |
| `--cors`     | Dodatkowe dozwolone źródła CORS            |

---

### acp

Uruchom serwer ACP (Agent Client Protocol).

```bash
modtools acp
```

Uruchamia serwer ACP, który komunikuje się przez stdin/stdout przy użyciu JSON-RPC.

#### Flagi

| Flaga        | Opis                                       |
| ------------ | ------------------------------------------ |
| `--cwd`      | Katalog roboczy                            |
| `--port`     | Port do nasłuchiwania                      |
| `--hostname` | Nazwa hosta, do której należy się powiązać |

---

### uninstall

Odinstaluj Model-of-Design Tools i usuń wszystkie powiązane pliki.

```bash
modtools uninstall
```

#### Flagi

| Flaga           | Skrót | Opis                          |
| --------------- | ----- | ----------------------------- |
| `--keep-config` | `-c`  | Zachowaj pliki konfiguracyjne |
| `--keep-data`   | `-d`  | Zachowaj dane sesji i migawki |
| `--dry-run`     |       | Pokaż co zostanie usunięte    |
| `--force`       | `-f`  | Pomiń monity o potwierdzenie  |

---

### upgrade

Aktualizuj Model-of-Design Tools do najnowszej lub określonej wersji.

```bash
modtools upgrade [target]
```

Aby zaktualizować do najnowszej wersji:

```bash
modtools upgrade
```

Aby zaktualizować do konkretnej wersji:

```bash
modtools upgrade v0.1.48
```

#### Flagi

| Flaga      | Skrót | Opis                                                |
| ---------- | ----- | --------------------------------------------------- |
| `--method` | `-m`  | Wymuś metodę instalacji: curl, npm, pnpm, bun, brew |

---

## Flagi globalne

Interfejs CLI Model-of-Design Tools przyjmuje następujące flagi globalne dla każdego polecenia.

| Flaga          | Skrót | Opis                                        |
| -------------- | ----- | ------------------------------------------- |
| `--help`       | `-h`  | Wyświetl pomoc                              |
| `--version`    | `-v`  | Wydrukuj numer wersji                       |
| `--print-logs` |       | Drukuj logi na stderr                       |
| `--log-level`  |       | Poziom logowania (DEBUG, INFO, WARN, ERROR) |

---

## Zmienne środowiskowe

Model-of-Design Tools można skonfigurować za pomocą zmiennych środowiskowych.

| Zmienna                               | Typ     | Opis                                                        |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `MOD_AUTO_SHARE`                 | boolean | Automatycznie udostępniaj sesje                             |
| `MOD_GIT_BASH_PATH`              | string  | Ścieżka do pliku wykonywalnego Git Bash w systemie Windows  |
| `MOD_CONFIG`                     | string  | Ścieżka do pliku konfiguracyjnego                           |
| `MOD_TUI_CONFIG`                 | string  | Ścieżka do pliku konfiguracyjnego TUI                       |
| `MOD_CONFIG_DIR`                 | string  | Ścieżka do katalogu konfiguracyjnego                        |
| `MOD_CONFIG_CONTENT`             | string  | Treść konfiguracji JSON (inline)                            |
| `MOD_DISABLE_AUTOUPDATE`         | boolean | Wyłącz automatyczne sprawdzanie aktualizacji                |
| `MOD_DISABLE_PRUNE`              | boolean | Wyłącz czyszczenie starych wyników (pruning)                |
| `MOD_DISABLE_TERMINAL_TITLE`     | boolean | Wyłącz automatyczne ustawianie tytułu terminala             |
| `MOD_PERMISSION`                 | string  | Konfiguracja uprawnień w JSON (inline)                      |
| `MOD_DISABLE_DEFAULT_PLUGINS`    | boolean | Wyłącz domyślne wtyczki                                     |
| `MOD_DISABLE_LSP_DOWNLOAD`       | boolean | Wyłącz automatyczne pobieranie serwerów LSP                 |
| `MOD_ENABLE_EXPERIMENTAL_MODELS` | boolean | Włącz modele eksperymentalne                                |
| `MOD_DISABLE_AUTOCOMPACT`        | boolean | Wyłącz automatyczne kompaktowanie kontekstu                 |
| `MOD_DISABLE_CLAUDE_CODE`        | boolean | Wyłącz integrację z `.claude` (prompt + skills)             |
| `MOD_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Wyłącz czytanie `~/.claude/CLAUDE.md`                       |
| `MOD_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Wyłącz ładowanie `.claude/skills`                           |
| `MOD_DISABLE_MODELS_FETCH`       | boolean | Wyłącz pobieranie modeli ze źródeł zewnętrznych             |
| `MOD_FAKE_VCS`                   | string  | Fałszywy dostawca VCS do celów testowych                    |
| `MOD_DISABLE_FILETIME_CHECK`     | boolean | Wyłącz sprawdzanie czasu modyfikacji plików (optymalizacja) |
| `MOD_CLIENT`                     | string  | Identyfikator klienta (domyślnie `cli`)                     |
| `MOD_ENABLE_EXA`                 | boolean | Włącz narzędzie wyszukiwania internetowego Exa              |
| `MOD_SERVER_PASSWORD`            | string  | Włącz uwierzytelnianie podstawowe dla `serve`/`web`         |
| `MOD_SERVER_USERNAME`            | string  | Nazwa użytkownika do autoryzacji (domyślnie `modtools`)     |
| `MOD_MODELS_URL`                 | string  | Niestandardowy adres URL do pobierania konfiguracji modeli  |

---

### Eksperymentalne

Te zmienne włączają funkcje eksperymentalne, które mogą ulec zmianie lub zostać usunięte.

| Zmienna                                         | Typ     | Opis                                         |
| ----------------------------------------------- | ------- | -------------------------------------------- |
| `MOD_EXPERIMENTAL`                         | boolean | Włącz wszystkie funkcje eksperymentalne      |
| `MOD_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Włącz wykrywanie ikon                        |
| `MOD_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Wyłącz kopiowanie przy zaznaczaniu w TUI     |
| `MOD_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Domyślny limit czasu dla narzędzia bash w ms |
| `MOD_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksymalne tokeny wyjściowe dla LLM          |
| `MOD_EXPERIMENTAL_FILEWATCHER`             | boolean | Włącz obserwatora plików dla całego katalogu |
| `MOD_EXPERIMENTAL_OXFMT`                   | boolean | Włącz formater oxfmt                         |
| `MOD_EXPERIMENTAL_LSP_TOOL`                | boolean | Włącz eksperymentalne narzędzie LSP          |
| `MOD_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Wyłącz obserwatora plików                    |
| `MOD_EXPERIMENTAL_EXA`                     | boolean | Włącz funkcje eksperymentalne Exa            |
| `MOD_EXPERIMENTAL_LSP_TY`                  | boolean | Włącz TY LSP dla plików python               |
| `MOD_EXPERIMENTAL_MARKDOWN`                | boolean | Włącz funkcje eksperymentalne Markdown       |
| `MOD_EXPERIMENTAL_PLAN_MODE`               | boolean | Włącz tryb planowania                        |
