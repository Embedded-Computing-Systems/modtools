---
title: CLI
description: Model-of-Design Tools CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Model-of-Design Tools CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
modtools
```

But it also accepts commands as documented on this page. This allows you to interact with Model-of-Design Tools programmatically.

```bash
modtools run "Explain how closures work in JavaScript"
```

---

### tui

Start the Model-of-Design Tools terminal user interface.

```bash
modtools [project]
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--prompt`   |       | Prompt to use                                                           |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--port`     |       | Port to listen on                                                       |
| `--hostname` |       | Hostname to listen on                                                   |

---

## Commands

The Model-of-Design Tools CLI also has the following commands.

---

### agent

Manage agents for Model-of-Design Tools.

```bash
modtools agent [command]
```

---

### attach

Attach a terminal to an already running Model-of-Design Tools backend server started via `serve` or `web` commands.

```bash
modtools attach [url]
```

This allows using the TUI with a remote Model-of-Design Tools backend. For example:

```bash
# Start the backend server for web/mobile access
modtools web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
modtools attach http://10.20.30.40:4096
```

#### Flags

| Flag        | Short | Description                       |
| ----------- | ----- | --------------------------------- |
| `--dir`     |       | Working directory to start TUI in |
| `--session` | `-s`  | Session ID to continue            |

---

#### create

Create a new agent with custom configuration.

```bash
modtools agent create
```

This command will guide you through creating a new agent with a custom system prompt and tool configuration.

---

#### list

List all available agents.

```bash
modtools agent list
```

---

### auth

Command to manage credentials and login for providers.

```bash
modtools auth [command]
```

---

#### login

Model-of-Design Tools is powered by the provider list at [Models.dev](https://models.dev), so you can use `modtools auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/modtools/auth.json`.

```bash
modtools auth login
```

When Model-of-Design Tools starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
modtools auth list
```

Or the short version.

```bash
modtools auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
modtools auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
modtools github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
modtools github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
modtools github run
```

##### Flags

| Flag      | Description                            |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event to run the agent for |
| `--token` | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
modtools mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
modtools mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
modtools mcp list
```

Or use the short version.

```bash
modtools mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
modtools mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
modtools mcp auth list
```

Or use the short version.

```bash
modtools mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
modtools mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
modtools mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
modtools models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
modtools models anthropic
```

#### Flags

| Flag        | Description                                                  |
| ----------- | ------------------------------------------------------------ |
| `--refresh` | Refresh the models cache from models.dev                     |
| `--verbose` | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Model-of-Design Tools.

```bash
modtools models --refresh
```

---

### run

Run modtools in non-interactive mode by passing a prompt directly.

```bash
modtools run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "modtools run"
modtools run Explain the use of context in Go
```

You can also attach to a running `modtools serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
modtools serve

# In another terminal, run commands that attach to it
modtools run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--command`  |       | The command to run, use message for args                                |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--share`    |       | Share the session                                                       |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--file`     | `-f`  | File(s) to attach to message                                            |
| `--format`   |       | Format: default (formatted) or json (raw JSON events)                   |
| `--title`    |       | Title for the session (uses truncated prompt if no value provided)      |
| `--attach`   |       | Attach to a running modtools server (e.g., http://localhost:4096)       |
| `--port`     |       | Port for the local server (defaults to random port)                     |

---

### serve

Start a headless Model-of-Design Tools server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
modtools serve
```

This starts an HTTP server that provides API access to modtools functionality without the TUI interface. Set `MOD_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `modtools`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### session

Manage Model-of-Design Tools sessions.

```bash
modtools session [command]
```

---

#### list

List all Model-of-Design Tools sessions.

```bash
modtools session list
```

##### Flags

| Flag          | Short | Description                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limit to N most recent sessions      |
| `--format`    |       | Output format: table or json (table) |

---

### stats

Show token usage and cost statistics for your Model-of-Design Tools sessions.

```bash
modtools stats
```

#### Flags

| Flag        | Description                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Show stats for the last N days (all time)                                   |
| `--tools`   | Number of tools to show (all)                                               |
| `--models`  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| `--project` | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
modtools export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

---

### import

Import session data from a JSON file or Model-of-Design Tools share URL.

```bash
modtools import <file>
```

You can import from a local file or an Model-of-Design Tools share URL.

```bash
modtools import session.json
modtools import https://opncd.ai/s/abc123
```

---

### web

Start a headless Model-of-Design Tools server with a web interface.

```bash
modtools web
```

This starts an HTTP server and opens a web browser to access Model-of-Design Tools through a web interface. Set `MOD_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `modtools`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
modtools acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag         | Description           |
| ------------ | --------------------- |
| `--cwd`      | Working directory     |
| `--port`     | Port to listen on     |
| `--hostname` | Hostname to listen on |

---

### uninstall

Uninstall Model-of-Design Tools and remove all related files.

```bash
modtools uninstall
```

#### Flags

| Flag            | Short | Description                                 |
| --------------- | ----- | ------------------------------------------- |
| `--keep-config` | `-c`  | Keep configuration files                    |
| `--keep-data`   | `-d`  | Keep session data and snapshots             |
| `--dry-run`     |       | Show what would be removed without removing |
| `--force`       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates modtools to the latest version or a specific version.

```bash
modtools upgrade [target]
```

To upgrade to the latest version.

```bash
modtools upgrade
```

To upgrade to a specific version.

```bash
modtools upgrade v0.1.48
```

#### Flags

| Flag       | Short | Description                                                       |
| ---------- | ----- | ----------------------------------------------------------------- |
| `--method` | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The modtools CLI takes the following global flags.

| Flag           | Short | Description                          |
| -------------- | ----- | ------------------------------------ |
| `--help`       | `-h`  | Display help                         |
| `--version`    | `-v`  | Print version number                 |
| `--print-logs` |       | Print logs to stderr                 |
| `--log-level`  |       | Log level (DEBUG, INFO, WARN, ERROR) |

---

## Environment variables

Model-of-Design Tools can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `MOD_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `MOD_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `MOD_CONFIG`                     | string  | Path to config file                               |
| `MOD_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `MOD_CONFIG_DIR`                 | string  | Path to config directory                          |
| `MOD_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `MOD_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `MOD_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `MOD_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `MOD_PERMISSION`                 | string  | Inlined json permissions config                   |
| `MOD_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `MOD_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `MOD_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `MOD_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `MOD_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `MOD_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `MOD_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `MOD_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `MOD_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `MOD_DISABLE_FILETIME_CHECK`     | boolean | Disable file time checking for optimization       |
| `MOD_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `MOD_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `MOD_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `MOD_SERVER_USERNAME`            | string  | Override basic auth username (default `modtools`) |
| `MOD_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `MOD_EXPERIMENTAL`                         | boolean | Enable all experimental features        |
| `MOD_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `MOD_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `MOD_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `MOD_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `MOD_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `MOD_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `MOD_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `MOD_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `MOD_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `MOD_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `MOD_EXPERIMENTAL_MARKDOWN`                | boolean | Enable experimental markdown features   |
| `MOD_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
