---
title: CLI
description: Model-of-Design Tools CLI opcije i naredbe.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Model-of-Design Tools CLI po defaultu pokreće [TUI](/docs/tui) kada se pokrene bez ikakvih argumenata.

```bash
modtools
```

Ali takođe prihvata naredbe kao što je dokumentovano na ovoj stranici. Ovo vam omogućava programsku interakciju sa Model-of-Design Tools.

```bash
modtools run "Explain how closures work in JavaScript"
```

---

### tui

Pokrenite Model-of-Design Tools terminalski korisnički interfejs.

```bash
modtools [project]
```

#### Opcije

| Opcija       | Kratko | Opis                                                                     |
| ------------ | ------ | ------------------------------------------------------------------------ |
| `--continue` | `-c`   | Nastavite posljednju sesiju                                              |
| `--session`  | `-s`   | ID sesije za nastavak                                                    |
| `--fork`     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| `--prompt`   |        | Prompt za upotrebu                                                       |
| `--model`    | `-m`   | Model za korištenje u obliku provider/model                              |
| `--agent`    |        | Agent za korištenje                                                      |
| `--port`     |        | Port na kojem treba slušati                                              |
| `--hostname` |        | Hostname na kojem treba slušati                                          |

---

## Naredbe

Model-of-Design Tools CLI takođe ima sljedeće naredbe.

---

### agent

Upravljajte Model-of-Design Tools agentima.

```bash
modtools agent [command]
```

---

### attach

Priključite terminal na već pokrenut Model-of-Design Tools backend server pokrenut putem `serve` ili `web` naredbi.

```bash
modtools attach [url]
```

Ovo omogućava korištenje TUI-ja sa udaljenim Model-of-Design Tools backend-om. Na primjer:

```bash
# Start the backend server for web/mobile access
modtools web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
modtools attach http://10.20.30.40:4096
```

#### Opcije

| Opcija      | Kratko | Opis                                 |
| ----------- | ------ | ------------------------------------ |
| `--dir`     |        | Radni direktorij za pokretanje TUI-a |
| `--session` | `-s`   | ID sesije za nastavak                |

---

#### create

Kreirajte novog agenta s prilagođenom konfiguracijom.

```bash
modtools agent create
```

Ova naredba će vas voditi kroz kreiranje novog agenta sa prilagođenim sistemskim promptom i konfiguracijom alata.

---

#### list

Navedite sve dostupne agente.

```bash
modtools agent list
```

---

### auth

Naredba za upravljanje vjerodajnicama i prijavom za provajdere.

```bash
modtools auth [command]
```

---

#### login

Model-of-Design Tools pokreće lista provajdera na [Models.dev](https://models.dev), tako da možete koristiti `modtools auth login` da konfigurirate API ključeve za bilo kojeg provajdera kojeg želite koristiti. Ovo je pohranjeno u `~/.local/share/modtools/auth.json`.

```bash
modtools auth login
```

Kada se Model-of-Design Tools pokrene, učitava dobavljače iz datoteke vjerodajnica. I ako postoje neki ključevi definirani u vašim okruženjima ili `.env` fajl u vašem projektu.

---

#### list

Navodi sve autentifikovane dobavljače pohranjene u datoteci vjerodajnica.

```bash
modtools auth list
```

Ili kratka verzija.

```bash
modtools auth ls
```

---

#### logout

Odjavljuje vas s provajdera tako što ga briše iz datoteke vjerodajnica.

```bash
modtools auth logout
```

---

### github

Upravljajte GitHub agentom za automatizaciju repozitorija.

```bash
modtools github [command]
```

---

#### install

Instalirajte GitHub agenta u svoj repozitorij.

```bash
modtools github install
```

Ovo postavlja neophodni tok rada GitHub Actions i vodi vas kroz proces konfiguracije. [Saznajte više](/docs/github).

---

#### run

Pokrenite GitHub agent. Ovo se obično koristi u GitHub Actions.

```bash
modtools github run
```

##### Opcije

| Opcija    | Opis                                   |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event za pokretanje agenta |
| `--token` | GitHub Personal Access Token           |

---

### mcp

Upravljajte Model Context Protocol (MCP) serverima.

```bash
modtools mcp [command]
```

---

#### add

Dodajte MCP server svojoj konfiguraciji.

```bash
modtools mcp add
```

Ova naredba će vas voditi kroz dodavanje lokalnog ili udaljenog MCP servera.

---

#### list

Navedite sve konfigurirane MCP servere i njihov status veze.

```bash
modtools mcp list
```

Ili koristite kratku verziju.

```bash
modtools mcp ls
```

---

#### auth

Autentifikujte se sa MCP serverom koji je omogućen za OAuth.

```bash
modtools mcp auth [name]
```

Ako ne navedete ime servera, od vas će biti zatraženo da izaberete neki od dostupnih servera koji podržavaju OAuth.
Također možete navesti servere koji podržavaju OAuth i njihov status autentifikacije.

```bash
modtools mcp auth list
```

Ili koristite kratku verziju.

```bash
modtools mcp auth ls
```

---

#### logout

Uklonite OAuth vjerodajnice za MCP server.

```bash
modtools mcp logout [name]
```

---

#### debug

Otklanjanje grešaka (debug) OAuth veze sa MCP serverom.

```bash
modtools mcp debug <name>
```

---

### models

Navedite sve dostupne modele konfiguriranih provajdera.

```bash
modtools models [provider]
```

Ova naredba prikazuje sve modele dostupne kod vaših konfiguriranih provajdera u formatu `provider/model`.
Ovo je korisno za pronalaženje tačnog naziva modela za korištenje u [vašoj konfiguraciji](/docs/config/).
Opciono možete proslijediti ID provajdera za filtriranje modela po tom dobavljaču.

```bash
modtools models anthropic
```

#### Opcije

| Opcija      | Opis                                                                     |
| ----------- | ------------------------------------------------------------------------ |
| `--refresh` | Osvježite keš modela sa models.dev                                       |
| `--verbose` | Koristite detaljniji izlaz modela (uključuje metapodatke poput troškova) |

Koristite `--refresh` zastavicu da ažurirate keširanu listu modela. Ovo je korisno kada su novi modeli dodani provajderu i želite da ih vidite u Model-of-Design Tools.

```bash
modtools models --refresh
```

---

### run

Pokrenite Model-of-Design Tools u neinteraktivnom modu tako što ćete direktno proslijediti prompt.

```bash
modtools run [message..]
```

Ovo je korisno za skriptiranje, automatizaciju ili kada želite brz odgovor bez pokretanja punog TUI-ja. Na primjer:

```bash "modtools run"
modtools run Explain the use of context in Go
```

Također možete priključiti pokrenutu `modtools serve` instancu kako biste izbjegli vrijeme hladnog pokretanja MCP servera pri svakom pokretanju:

```bash
# Start a headless server in one terminal
modtools serve

# In another terminal, run commands that attach to it
modtools run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opcije

| Opcija       | Kratko | Opis                                                                     |
| ------------ | ------ | ------------------------------------------------------------------------ |
| `--command`  |        | Naredba za pokretanje, koristite poruku za argumente                     |
| `--continue` | `-c`   | Nastavite posljednju sesiju                                              |
| `--session`  | `-s`   | ID sesije za nastavak                                                    |
| `--fork`     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| `--share`    |        | Podijelite sesiju                                                        |
| `--model`    | `-m`   | Model za korištenje u obliku provider/model                              |
| `--agent`    |        | Agent za korištenje                                                      |
| `--file`     | `-f`   | Fajlovi koje treba priložiti poruci                                      |
| `--format`   |        | Format: default (formatiran) ili json (sirovi JSON događaji)             |
| `--title`    |        | Naslov sesije (koristi skraćeni prompt ako nije navedena vrijednost)     |
| `--attach`   |        | Priključite na pokrenuti Model-of-Design Tools server (npr. http://localhost:4096)    |
| `--port`     |        | Port za lokalni server (zadano na nasumični port)                        |

---

### serve

Pokrenite Model-of-Design Tools headless server za API pristup. Pogledajte [server docs](/docs/server) za kompletan HTTP interfejs.

```bash
modtools serve
```

Ovo pokreće HTTP server koji pruža API pristup funkcionalnosti Model-of-Design Tools-a bez TUI interfejsa. Postavite `MOD_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `modtools`).

#### Opcije

| Opcija       | Opis                                                  |
| ------------ | ----------------------------------------------------- |
| `--port`     | Port na kojem treba slušati                           |
| `--hostname` | Hostname na kojem treba slušati                       |
| `--mdns`     | Omogući mDNS otkrivanje                               |
| `--cors`     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### session

Upravljajte Model-of-Design Tools sesijama.

```bash
modtools session [command]
```

---

#### list

Navedite sve Model-of-Design Tools sesije.

```bash
modtools session list
```

##### Opcije

| Opcija        | Kratko | Opis                                   |
| ------------- | ------ | -------------------------------------- |
| `--max-count` | `-n`   | Ograničenje na N najnovijih sesija     |
| `--format`    |        | Izlazni format: table ili json (table) |

---

### stats

Prikaži statistiku upotrebe tokena i troškova za vaše Model-of-Design Tools sesije.

```bash
modtools stats
```

#### Opcije

| Opcija      | Opis                                                                                                       |
| ----------- | ---------------------------------------------------------------------------------------------------------- |
| `--days`    | Prikaži statistiku za zadnjih N dana (sva vremena)                                                         |
| `--tools`   | Broj alata za prikaz (svi)                                                                                 |
| `--models`  | Prikaži raščlambu korištenja modela (skriveno prema zadanim postavkama). Proslijedite broj za prikaz top N |
| `--project` | Filtriraj po projektu (svi projekti, prazan niz: trenutni projekt)                                         |

---

### export

Izvezite podatke sesije kao JSON.

```bash
modtools export [sessionID]
```

Ako ne unesete ID sesije, od vas će biti zatraženo da odaberete neku od dostupnih sesija.

---

### import

Uvezite podatke sesije iz JSON datoteke ili Model-of-Design Tools dijeljenog URL-a.

```bash
modtools import <file>
```

Možete uvesti iz lokalne datoteke ili Model-of-Design Tools dijeljenog URL-a.

```bash
modtools import session.json
modtools import https://opncd.ai/s/abc123
```

---

### web

Pokrenite Model-of-Design Tools headless server sa web interfejsom.

```bash
modtools web
```

Ovo pokreće HTTP server i otvara web pretraživač za pristup Model-of-Design Tools-u preko web interfejsa. Postavite `MOD_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `modtools`).

#### Opcije

| Opcija       | Opis                                                  |
| ------------ | ----------------------------------------------------- |
| `--port`     | Port na kojem treba slušati                           |
| `--hostname` | Hostname na kojem treba slušati                       |
| `--mdns`     | Omogući mDNS otkrivanje                               |
| `--cors`     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### acp

Pokrenite ACP (Agent Client Protocol) server.

```bash
modtools acp
```

Ova naredba pokreće ACP server koji komunicira preko stdin/stdout koristeći nd-JSON.

#### Opcije

| Opcija       | Opis                        |
| ------------ | --------------------------- |
| `--cwd`      | Radni direktorij            |
| `--port`     | Port na kojem treba slušati |
| `--hostname` | Hostname na kojem slušati   |

---

### uninstall

Deinstalirajte Model-of-Design Tools i uklonite sve povezane datoteke.

```bash
modtools uninstall
```

#### Opcije

| Opcija          | Kratko | Opis                                          |
| --------------- | ------ | --------------------------------------------- |
| `--keep-config` | `-c`   | Sačuvajte konfiguracijske datoteke            |
| `--keep-data`   | `-d`   | Sačuvajte podatke i snimke sesije             |
| `--dry-run`     |        | Pokažite šta bi bilo uklonjeno bez uklanjanja |
| `--force`       | `-f`   | Preskoči upite za potvrdu                     |

---

### upgrade

Ažurira Model-of-Design Tools na najnoviju verziju ili određenu verziju.

```bash
modtools upgrade [target]
```

Za nadogradnju na najnoviju verziju.

```bash
modtools upgrade
```

Za nadogradnju na određenu verziju.

```bash
modtools upgrade v0.1.48
```

#### Opcije

| Opcija     | Kratko | Opis                                                    |
| ---------- | ------ | ------------------------------------------------------- |
| `--method` | `-m`   | Korišteni način instalacije; curl, npm, pnpm, bun, brew |

---

## Globalne opcije

Model-of-Design Tools CLI prihvata sljedeće globalne zastavice.

| Opcija         | Kratko | Opis                                      |
| -------------- | ------ | ----------------------------------------- |
| `--help`       | `-h`   | Prikaži pomoć                             |
| `--version`    | `-v`   | Ispiši broj verzije                       |
| `--print-logs` |        | Ispis logova u stderr                     |
| `--log-level`  |        | Nivo logovanja (DEBUG, INFO, WARN, ERROR) |

---

## Varijable okruženja

Model-of-Design Tools se može konfigurirati pomoću varijabli okruženja.

| Varijabla                             | Tip     | Opis                                                               |
| ------------------------------------- | ------- | ------------------------------------------------------------------ |
| `MOD_AUTO_SHARE`                 | boolean | Automatski dijeli sesije                                           |
| `MOD_GIT_BASH_PATH`              | string  | Putanja do Git Bash izvršne datoteke na Windows-u                  |
| `MOD_CONFIG`                     | string  | Putanja do konfiguracijskog fajla                                  |
| `MOD_TUI_CONFIG`                 | string  | Putanja do TUI konfiguracijskog fajla                              |
| `MOD_CONFIG_DIR`                 | string  | Putanja do konfiguracijskog direktorija                            |
| `MOD_CONFIG_CONTENT`             | string  | Inline json konfiguracijski sadržaj                                |
| `MOD_DISABLE_AUTOUPDATE`         | boolean | Onemogući automatske provjere ažuriranja                           |
| `MOD_DISABLE_PRUNE`              | boolean | Onemogući brisanje (pruning) starih podataka                       |
| `MOD_DISABLE_TERMINAL_TITLE`     | boolean | Onemogući automatsko ažuriranje naslova terminala                  |
| `MOD_PERMISSION`                 | string  | Inline json konfiguracija dozvola                                  |
| `MOD_DISABLE_DEFAULT_PLUGINS`    | boolean | Onemogući podrazumijevane dodatke (plugins)                        |
| `MOD_DISABLE_LSP_DOWNLOAD`       | boolean | Onemogući automatsko preuzimanje LSP servera                       |
| `MOD_ENABLE_EXPERIMENTAL_MODELS` | boolean | Omogući eksperimentalne modele                                     |
| `MOD_DISABLE_AUTOCOMPACT`        | boolean | Onemogući automatsko sažimanje konteksta                           |
| `MOD_DISABLE_CLAUDE_CODE`        | boolean | Onemogući čitanje iz `.claude` (prompt + vještine)                 |
| `MOD_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Onemogući čitanje `~/.claude/CLAUDE.md`                            |
| `MOD_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Onemogući učitavanje `.claude/skills`                              |
| `MOD_DISABLE_MODELS_FETCH`       | boolean | Onemogući dohvaćanje modela iz udaljenih izvora                    |
| `MOD_FAKE_VCS`                   | string  | Lažni VCS provajder za potrebe testiranja                          |
| `MOD_DISABLE_FILETIME_CHECK`     | boolean | Onemogući provjeru vremena datoteke radi optimizacije              |
| `MOD_CLIENT`                     | string  | Identifikator klijenta (zadano na `cli`)                           |
| `MOD_ENABLE_EXA`                 | boolean | Omogući Exa alate za web pretraživanje                             |
| `MOD_SERVER_PASSWORD`            | string  | Omogući osnovnu autentifikaciju za `serve`/`web`                   |
| `MOD_SERVER_USERNAME`            | string  | Poništi osnovno korisničko ime autentifikacije (zadano `modtools`) |
| `MOD_MODELS_URL`                 | string  | Prilagođeni URL za dohvaćanje konfiguracije modela                 |

---

### Eksperimentalno

Ove varijable okruženja omogućavaju eksperimentalne karakteristike koje se mogu promijeniti ili ukloniti.

| Varijabla                                       | Tip     | Opis                                              |
| ----------------------------------------------- | ------- | ------------------------------------------------- |
| `MOD_EXPERIMENTAL`                         | boolean | Omogući sve eksperimentalne funkcije              |
| `MOD_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Omogući otkrivanje ikona                          |
| `MOD_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Onemogući kopiranje pri odabiru u TUI             |
| `MOD_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Zadano vremensko ograničenje za bash naredbe u ms |
| `MOD_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksimalni izlazni tokeni za LLM odgovore         |
| `MOD_EXPERIMENTAL_FILEWATCHER`             | boolean | Omogući praćenje datoteka za cijeli direktorij    |
| `MOD_EXPERIMENTAL_OXFMT`                   | boolean | Omogući oxfmt formatter                           |
| `MOD_EXPERIMENTAL_LSP_TOOL`                | boolean | Omogući eksperimentalni LSP alat                  |
| `MOD_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Onemogući praćenje datoteka                       |
| `MOD_EXPERIMENTAL_EXA`                     | boolean | Omogući eksperimentalne Exa funkcije              |
| `MOD_EXPERIMENTAL_LSP_TY`                  | boolean | Omogući TY LSP za python datoteke                 |
| `MOD_EXPERIMENTAL_MARKDOWN`                | boolean | Omogući eksperimentalne Markdown funkcije         |
| `MOD_EXPERIMENTAL_PLAN_MODE`               | boolean | Omogući Plan mod                                  |
