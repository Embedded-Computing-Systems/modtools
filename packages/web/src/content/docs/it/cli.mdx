---
title: CLI
description: Opzioni e comandi della CLI di Model-of-Design Tools.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI di Model-of-Design Tools, per impostazione predefinita, avvia la [TUI](/docs/tui) quando viene eseguita senza argomenti.

```bash
modtools
```

Ma accetta anche i comandi documentati in questa pagina. Questo ti permette di interagire con Model-of-Design Tools in modo programmatico.

```bash
modtools run "Explain how closures work in JavaScript"
```

---

### tui

Avvia l'interfaccia testuale di Model-of-Design Tools (interfaccia utente da terminale).

```bash
modtools [project]
```

#### Flag

| Flag         | Breve | Descrizione                                                              |
| ------------ | ----- | ------------------------------------------------------------------------ |
| `--continue` | `-c`  | Continua l'ultima sessione                                               |
| `--session`  | `-s`  | ID sessione da continuare                                                |
| `--fork`     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`) |
| `--prompt`   |       | Prompt da usare                                                          |
| `--model`    | `-m`  | Modello nel formato provider/model                                       |
| `--agent`    |       | Agente da usare                                                          |
| `--port`     |       | Porta su cui mettersi in ascolto                                         |
| `--hostname` |       | Hostname su cui mettersi in ascolto                                      |

---

## Comandi

La CLI di Model-of-Design Tools include anche i seguenti comandi.

---

### agent

Gestisci gli agenti per Model-of-Design Tools.

```bash
modtools agent [command]
```

---

### attach

Collega un terminale a un backend server di Model-of-Design Tools già in esecuzione avviato tramite i comandi `serve` o `web`.

```bash
modtools attach [url]
```

Questo consente di usare la TUI con un backend Model-of-Design Tools remoto. Per esempio:

```bash
# Start the backend server for web/mobile access
modtools web --port 4096 --hostname 0.0.0.0

# In un altro terminale, collega la TUI al backend in esecuzione
modtools attach http://10.20.30.40:4096
```

#### Flag

| Flag        | Breve | Descrizione                             |
| ----------- | ----- | --------------------------------------- |
| `--dir`     |       | Working directory in cui avviare la TUI |
| `--session` | `-s`  | ID sessione da continuare               |

---

#### create

Crea un nuovo agente con configurazione personalizzata.

```bash
modtools agent create
```

Questo comando ti guida nella creazione di un nuovo agente con un system prompt personalizzato e configurazione degli strumenti.

---

#### list

Elenca tutti gli agenti disponibili.

```bash
modtools agent list
```

---

### auth

Comando per gestire le credenziali e il login dei provider.

```bash
modtools auth [command]
```

---

#### login

Model-of-Design Tools si basa sull'elenco provider di [Models.dev](https://models.dev), quindi puoi usare `modtools auth login` per configurare le API key per qualunque provider tu voglia usare. Le credenziali vengono salvate in `~/.local/share/modtools/auth.json`.

```bash
modtools auth login
```

Quando Model-of-Design Tools si avvia, carica i provider dal file delle credenziali e, se presenti, anche eventuali key definite nell'ambiente o in un file `.env` nel progetto.

---

#### list

Elenca tutti i provider autenticati come salvati nel file delle credenziali.

```bash
modtools auth list
```

Oppure la versione corta.

```bash
modtools auth ls
```

---

#### logout

Esegue il logout da un provider rimuovendolo dal file delle credenziali.

```bash
modtools auth logout
```

---

### github

Gestisci l'agente GitHub per l'automazione dei repository.

```bash
modtools github [command]
```

---

#### install

Installa l'agente GitHub nel tuo repository.

```bash
modtools github install
```

Questo configura il workflow GitHub Actions necessario e ti guida nel processo di configurazione. [Scopri di più](/docs/github).

---

#### run

Esegui l'agente GitHub. Tipicamente usato in GitHub Actions.

```bash
modtools github run
```

##### Flag

| Flag      | Descrizione                                  |
| --------- | -------------------------------------------- |
| `--event` | Evento GitHub mock per cui eseguire l'agente |
| `--token` | GitHub personal access token                 |

---

### mcp

Gestisci i server Model Context Protocol.

```bash
modtools mcp [command]
```

---

#### add

Aggiungi un server MCP alla tua configurazione.

```bash
modtools mcp add
```

Questo comando ti guida nell'aggiunta di un server MCP locale o remoto.

---

#### list

Elenca tutti i server MCP configurati e il loro stato di connessione.

```bash
modtools mcp list
```

Oppure la versione corta.

```bash
modtools mcp ls
```

---

#### auth

Autentica con un server MCP con OAuth abilitato.

```bash
modtools mcp auth [name]
```

Se non fornisci un nome server, ti verrà chiesto di selezionare tra i server OAuth-capable disponibili.

Puoi anche elencare i server OAuth-capable e il loro stato di autenticazione.

```bash
modtools mcp auth list
```

Oppure la versione corta.

```bash
modtools mcp auth ls
```

---

#### logout

Rimuovi le credenziali OAuth per un server MCP.

```bash
modtools mcp logout [name]
```

---

#### debug

Esegui debug di problemi di connessione OAuth per un server MCP.

```bash
modtools mcp debug <name>
```

---

### models

Elenca tutti i modelli disponibili dai provider configurati.

```bash
modtools models [provider]
```

Questo comando mostra tutti i modelli disponibili tra i provider configurati nel formato `provider/model`.

È utile per capire l'esatto nome del modello da usare nella [config](/docs/config/).

Puoi anche passare opzionalmente un ID provider per filtrare i modelli a quel provider.

```bash
modtools models anthropic
```

#### Flag

| Flag        | Descrizione                                        |
| ----------- | -------------------------------------------------- |
| `--refresh` | Aggiorna la cache modelli da models.dev            |
| `--verbose` | Output più verboso (include metadati come i costi) |

Usa `--refresh` per aggiornare l'elenco modelli in cache. È utile quando nuovi modelli vengono aggiunti a un provider e vuoi vederli in Model-of-Design Tools.

```bash
modtools models --refresh
```

---

### run

Esegui modtools in modalità non interattiva passando un prompt direttamente.

```bash
modtools run [message..]
```

È utile per scripting, automazione o quando vuoi una risposta rapida senza avviare la TUI completa. Per esempio:

```bash "modtools run"
modtools run Explain the use of context in Go
```

Puoi anche collegarti a una istanza `modtools serve` già in esecuzione per evitare i cold boot dei server MCP ad ogni esecuzione:

```bash
# Avvia un server headless in un terminale
modtools serve

# In un altro terminale, esegui comandi che si collegano ad esso
modtools run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag         | Breve | Descrizione                                                              |
| ------------ | ----- | ------------------------------------------------------------------------ |
| `--command`  |       | Il comando da eseguire; usa message per gli argomenti                    |
| `--continue` | `-c`  | Continua l'ultima sessione                                               |
| `--session`  | `-s`  | ID sessione da continuare                                                |
| `--fork`     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`) |
| `--share`    |       | Condividi la sessione                                                    |
| `--model`    | `-m`  | Modello nel formato provider/model                                       |
| `--agent`    |       | Agente da usare                                                          |
| `--file`     | `-f`  | File da allegare al messaggio                                            |
| `--format`   |       | Formato: default (formattato) o json (eventi JSON grezzi)                |
| `--title`    |       | Titolo sessione (usa prompt troncato se non viene fornito un valore)     |
| `--attach`   |       | Attach a un server modtools in esecuzione (es. http://localhost:4096)    |
| `--port`     |       | Porta per il server locale (di default una porta casuale)                |

---

### serve

Avvia un server Model-of-Design Tools headless per accesso via API. Vedi le [server docs](/docs/server) per l'interfaccia HTTP completa.

```bash
modtools serve
```

Avvia un server HTTP che espone accesso API alle funzionalità di modtools senza la TUI. Imposta `MOD_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `modtools`).

#### Flag

| Flag         | Descrizione                                    |
| ------------ | ---------------------------------------------- |
| `--port`     | Porta su cui mettersi in ascolto               |
| `--hostname` | Hostname su cui mettersi in ascolto            |
| `--mdns`     | Abilita discovery mDNS                         |
| `--cors`     | Origin browser addizionali per consentire CORS |

---

### session

Gestisci le sessioni Model-of-Design Tools.

```bash
modtools session [command]
```

---

#### list

Elenca tutte le sessioni Model-of-Design Tools.

```bash
modtools session list
```

##### Flag

| Flag          | Breve | Descrizione                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limita alle N sessioni più recenti   |
| `--format`    |       | Formato output: table o json (table) |

---

### stats

Mostra statistiche di utilizzo token e costo per le sessioni Model-of-Design Tools.

```bash
modtools stats
```

#### Flag

| Flag        | Descrizione                                                                           |
| ----------- | ------------------------------------------------------------------------------------- |
| `--days`    | Mostra statistiche per gli ultimi N giorni (all time)                                 |
| `--tools`   | Numero di strumenti da mostrare (all)                                                 |
| `--models`  | Mostra breakdown di utilizzo modelli (nascosto di default). Passa un numero per top N |
| `--project` | Filtra per progetto (tutti i progetti; stringa vuota: progetto corrente)              |

---

### export

Esporta i dati di sessione come JSON.

```bash
modtools export [sessionID]
```

Se non fornisci un ID sessione, ti verrà chiesto di selezionare tra le sessioni disponibili.

---

### import

Importa i dati di sessione da un file JSON o da un URL di condivisione Model-of-Design Tools.

```bash
modtools import <file>
```

Puoi importare da un file locale o da un URL di condivisione Model-of-Design Tools.

```bash
modtools import session.json
modtools import https://opncd.ai/s/abc123
```

---

### web

Avvia un server Model-of-Design Tools headless con interfaccia web.

```bash
modtools web
```

Avvia un server HTTP e apre un browser per accedere a Model-of-Design Tools tramite interfaccia web. Imposta `MOD_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `modtools`).

#### Flag

| Flag         | Descrizione                                    |
| ------------ | ---------------------------------------------- |
| `--port`     | Porta su cui mettersi in ascolto               |
| `--hostname` | Hostname su cui mettersi in ascolto            |
| `--mdns`     | Abilita discovery mDNS                         |
| `--cors`     | Origin browser addizionali per consentire CORS |

---

### acp

Avvia un server ACP (Agent Client Protocol).

```bash
modtools acp
```

Questo comando avvia un server ACP che comunica via stdin/stdout usando nd-JSON.

#### Flag

| Flag         | Descrizione                         |
| ------------ | ----------------------------------- |
| `--cwd`      | Directory di lavoro                 |
| `--port`     | Porta su cui mettersi in ascolto    |
| `--hostname` | Hostname su cui mettersi in ascolto |

---

### uninstall

Disinstalla Model-of-Design Tools e rimuove tutti i file correlati.

```bash
modtools uninstall
```

#### Flag

| Flag            | Breve | Descrizione                                  |
| --------------- | ----- | -------------------------------------------- |
| `--keep-config` | `-c`  | Mantieni i file di configurazione            |
| `--keep-data`   | `-d`  | Mantieni dati di sessione e snapshot         |
| `--dry-run`     |       | Mostra cosa verrebbe rimosso senza rimuovere |
| `--force`       | `-f`  | Salta le richieste di conferma               |

---

### upgrade

Aggiorna modtools all'ultima versione o a una versione specifica.

```bash
modtools upgrade [target]
```

Per aggiornare all'ultima versione:

```bash
modtools upgrade
```

Per aggiornare a una versione specifica:

```bash
modtools upgrade v0.1.48
```

#### Flag

| Flag       | Breve | Descrizione                                               |
| ---------- | ----- | --------------------------------------------------------- |
| `--method` | `-m`  | Metodo di installazione usato: curl, npm, pnpm, bun, brew |

---

## Flag globali

La CLI di modtools accetta i seguenti flag globali.

| Flag           | Breve | Descrizione                            |
| -------------- | ----- | -------------------------------------- |
| `--help`       | `-h`  | Mostra l'help                          |
| `--version`    | `-v`  | Stampa il numero di versione           |
| `--print-logs` |       | Stampa i log su stderr                 |
| `--log-level`  |       | Livello log (DEBUG, INFO, WARN, ERROR) |

---

## Variabili d'ambiente

Model-of-Design Tools può essere configurato tramite variabili d'ambiente.

| Variabile                             | Tipo    | Descrizione                                                 |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `MOD_AUTO_SHARE`                 | boolean | Condivide automaticamente le sessioni                       |
| `MOD_GIT_BASH_PATH`              | string  | Percorso all'eseguibile Git Bash su Windows                 |
| `MOD_CONFIG`                     | string  | Percorso al file di configurazione                          |
| `MOD_TUI_CONFIG`                 | string  | Percorso al file di configurazione TUI                      |
| `MOD_CONFIG_DIR`                 | string  | Percorso alla directory di configurazione                   |
| `MOD_CONFIG_CONTENT`             | string  | Contenuto JSON di config inline                             |
| `MOD_DISABLE_AUTOUPDATE`         | boolean | Disabilita i controlli automatici di aggiornamento          |
| `MOD_DISABLE_PRUNE`              | boolean | Disabilita la potatura dei dati vecchi                      |
| `MOD_DISABLE_TERMINAL_TITLE`     | boolean | Disabilita aggiornamenti automatici del titolo terminale    |
| `MOD_PERMISSION`                 | string  | Config permessi JSON inline                                 |
| `MOD_DISABLE_DEFAULT_PLUGINS`    | boolean | Disabilita i plugin di default                              |
| `MOD_DISABLE_LSP_DOWNLOAD`       | boolean | Disabilita download automatico dei server LSP               |
| `MOD_ENABLE_EXPERIMENTAL_MODELS` | boolean | Abilita modelli sperimentali                                |
| `MOD_DISABLE_AUTOCOMPACT`        | boolean | Disabilita compaction automatica del contesto               |
| `MOD_DISABLE_CLAUDE_CODE`        | boolean | Disabilita lettura da `.claude` (prompt + skill)            |
| `MOD_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disabilita lettura di `~/.claude/CLAUDE.md`                 |
| `MOD_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disabilita caricamento di `.claude/skills`                  |
| `MOD_DISABLE_MODELS_FETCH`       | boolean | Disabilita fetch dei modelli da fonti remote                |
| `MOD_FAKE_VCS`                   | string  | Provider VCS finto per scopi di test                        |
| `MOD_DISABLE_FILETIME_CHECK`     | boolean | Disabilita controllo file time per ottimizzazione           |
| `MOD_CLIENT`                     | string  | Identificatore client (default `cli`)                       |
| `MOD_ENABLE_EXA`                 | boolean | Abilita gli strumenti di web search Exa                     |
| `MOD_SERVER_PASSWORD`            | string  | Abilita basic auth per `serve`/`web`                        |
| `MOD_SERVER_USERNAME`            | string  | Sovrascrive lo username basic auth (default `modtools`)     |
| `MOD_MODELS_URL`                 | string  | URL personalizzato per recuperare la configurazione modelli |

---

### Sperimentale

Queste variabili d'ambiente abilitano funzionalità sperimentali che potrebbero cambiare o essere rimosse.

| Variabile                                       | Tipo    | Descrizione                                |
| ----------------------------------------------- | ------- | ------------------------------------------ |
| `MOD_EXPERIMENTAL`                         | boolean | Abilita tutte le funzionalità sperimentali |
| `MOD_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Abilita icon discovery                     |
| `MOD_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disabilita copy on select nella TUI        |
| `MOD_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Timeout di default per comandi bash in ms  |
| `MOD_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Massimo token di output per risposte LLM   |
| `MOD_EXPERIMENTAL_FILEWATCHER`             | boolean | Abilita file watcher per l'intera dir      |
| `MOD_EXPERIMENTAL_OXFMT`                   | boolean | Abilita formatter oxfmt                    |
| `MOD_EXPERIMENTAL_LSP_TOOL`                | boolean | Abilita strumento LSP sperimentale         |
| `MOD_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disabilita file watcher                    |
| `MOD_EXPERIMENTAL_EXA`                     | boolean | Abilita funzionalità Exa sperimentali      |
| `MOD_EXPERIMENTAL_LSP_TY`                  | boolean | Abilita Abilita TY LSP per i file python   |
| `MOD_EXPERIMENTAL_MARKDOWN`                | boolean | Abilita markdown sperimentale              |
| `MOD_EXPERIMENTAL_PLAN_MODE`               | boolean | Abilita plan mode                          |
