---
title: CLI
description: Model-of-Design Tools CLI muligheder og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Model-of-Design Tools CLI starter som standard [TUI](/docs/tui), når den kører uden nogen argumenter.

```bash
modtools
```

Men den accepterer også kommandoer som dokumenteret på denne side. Dette giver dig mulighed for at interagere med Model-of-Design Tools programmatisk.

```bash
modtools run "Explain how closures work in JavaScript"
```

---

### tui

Start Model-of-Design Tools terminalbrugergrænsefladen.

```bash
modtools [project]
```

#### Flag

| Flag         | Kort | Beskrivelse                                                                  |
| ------------ | ---- | ---------------------------------------------------------------------------- |
| `--continue` | `-c` | Fortsæt sidste session                                                       |
| `--session`  | `-s` | Sessions-id for at fortsætte                                                 |
| `--fork`     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`) |
| `--prompt`   |      | Spørg om at bruge                                                            |
| `--model`    | `-m` | Model til brug i form af provider/model                                      |
| `--agent`    |      | Agent hos bruge                                                              |
| `--port`     |      | Port at lytte på                                                             |
| `--hostname` |      | Værtsnavn at lytte på                                                        |

---

## Kommandoer

Model-of-Design Tools CLI har også følgende kommandoer.

---

### agent

Administrer agenter i Model-of-Design Tools.

```bash
modtools agent [command]
```

---

### attach

Tilslut en terminal til en allerede kørende Model-of-Design Tools backend-server startet via `serve` eller `web` kommandoer.

```bash
modtools attach [url]
```

Dette gør det muligt at bruge TUI med en ekstern Model-of-Design Tools backend. For eksempel:

```bash
# Start the backend server for web/mobile access
modtools web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
modtools attach http://10.20.30.40:4096
```

#### Flag

| Flag        | Kort | Beskrivelse                      |
| ----------- | ---- | -------------------------------- |
| `--dir`     |      | Arbejdsmappe til at starte TUI i |
| `--session` | `-s` | Sessions-id for at fortsætte     |

---

#### create

Opret en ny agent med brugerdefineret konfiguration.

```bash
modtools agent create
```

Denne kommando vil guide dig gennem oprettelse af en ny agent med en brugerdefineret systemprompt og værktøjskonfiguration.

---

#### list

Liste over alle tilgængelige agenter.

```bash
modtools agent list
```

---

### auth

Kommando til at administrere legitimationsoplysninger og login for udbydere.

```bash
modtools auth [command]
```

---

#### login

Model-of-Design Tools drives af udbyderlisten på [Models.dev](https://models.dev), så du kan bruge `modtools auth login` til at konfigurere API-nøgler for enhver udbyder, du vil bruge. Dette er gemt i `~/.local/share/modtools/auth.json`.

```bash
modtools auth login
```

Når Model-of-Design Tools starter op, indlæser den udbyderne fra legitimationsfilen. Og hvis der er nogen nøgler defineret i dine miljøer eller en `.env` fil i dit projekt.

---

#### list

Viser alle de godkendte udbydere som gemt i legitimationsfilen.

```bash
modtools auth list
```

Eller den korte version.

```bash
modtools auth ls
```

---

#### logout

Logger dig ud af en udbyder ved at rydde den fra legitimationsfilen.

```bash
modtools auth logout
```

---

### github

Administrator GitHub-agenten til lagerautomatisering.

```bash
modtools github [command]
```

---

#### install

Installatør GitHub-agenten i dit lager.

```bash
modtools github install
```

Dette opsætter det nødvendige GitHub Actions workflow og guider dig gennem konfigurationsprocessen. [Learn more](/docs/github).

---

#### run

Kør GitHub-agenten. Dette bruges typisk i GitHub Actions.

```bash
modtools github run
```

##### Flag

| Flag      | Beskrivelse                                    |
| --------- | ---------------------------------------------- |
| `--event` | GitHub mock begivenhed for at køre agenten for |
| `--token` | GitHub personlig adgangstoken                  |

---

### mcp

Administrator Model Context Protocol-servere.

```bash
modtools mcp [command]
```

---

#### add

Tilføj en MCP-server til din konfiguration.

```bash
modtools mcp add
```

Denne kommando vil guide dig gennem tilføjelse af enten en lokal eller ekstern MCP-server.

---

#### list

Liste over alle konfigurerede MCP-servere og deres forbindelsesstatus.

```bash
modtools mcp list
```

Eller brug den korte version.

```bash
modtools mcp ls
```

---

#### auth

Godkendt med en OAuth-aktiveret MCP-server.

```bash
modtools mcp auth [name]
```

Hvis du ikke angiver et servernavn, bliver du bedt om at vælge blandt tilgængelige OAuth-kompatible servere.

Du kan også angive OAuth-kompatible servere og deres godkendelsesstatus.

```bash
modtools mcp auth list
```

Eller brug den korte version.

```bash
modtools mcp auth ls
```

---

#### logout

Fjern OAuth-legitimationsoplysninger for en MCP-server.

```bash
modtools mcp logout [name]
```

---

#### debug

Fejlfinding af OAuth-forbindelsesproblemer for en MCP-server.

```bash
modtools mcp debug <name>
```

---

### models

Liste over alle tilgængelige modeller fra konfigurerede udbydere.

```bash
modtools models [provider]
```

Denne kommando viser alle tilgængelige modeller på tværs af dine konfigurerede udbydere i formatet `provider/model`.

Dette er nyttigt til at finde ud af det nøjagtige modelnavn, der skal bruges i [your config](/docs/config/).

Du kan eventuelt videregive et udbyder-id for at filtrere modeller efter den udbyder.

```bash
modtools models anthropic
```

#### Flag

| Flag        | Beskrivelse                                                             |
| ----------- | ----------------------------------------------------------------------- |
| `--refresh` | Opdater modelcachen fra models.dev                                      |
| `--verbose` | Brug mere detaljeret modeloutput (inkluderer metadata som omkostninger) |

Brug flaget `--refresh` til at opdatere den cachelagrede modelliste. Dette er nyttigt, når nye modeller er blevet tilføjet til en udbyder, og du vil se dem i Model-of-Design Tools.

```bash
modtools models --refresh
```

---

### run

Kør modtools i ikke-interaktiv tilstand ved at sende en prompt direkte.

```bash
modtools run [message..]
```

Dette er nyttigt til scripting, automatisering, eller når du vil have et hurtigt svar uden at starte hele TUI. F.eks.

```bash "modtools run"
modtools run Explain the use of context in Go
```

Du kan også vedhæfte til en kørende `modtools serve`-instans for at undgå MCP serverens kolde opstartstider ved hver kørsel:

```bash
# Start a headless server in one terminal
modtools serve

# In another terminal, run commands that attach to it
modtools run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag         | Kort | Beskrivelse                                                                         |
| ------------ | ---- | ----------------------------------------------------------------------------------- |
| `--command`  |      | Kommandoen til at køre, brug besked til args                                        |
| `--continue` | `-c` | Fortsæt sidste session                                                              |
| `--session`  | `-s` | Sessions-id for at fortsætte                                                        |
| `--fork`     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`)        |
| `--share`    |      | Del sessionen                                                                       |
| `--model`    | `-m` | Model til brug i form af provider/model                                             |
| `--agent`    |      | Agent til brug                                                                      |
| `--file`     | `-f` | Fil(er), der skal vedhæftes til meddelelsen                                         |
| `--format`   |      | Format: standard (formateret) eller json (rå JSON hændelser)                        |
| `--title`    |      | Titel for sessionen (bruger trunkeret prompt, hvis der ikke er angivet nogen værdi) |
| `--attach`   |      | Tilslut til en kørende modtools-server (f.eks. http://localhost:4096)               |
| `--port`     |      | Port til den lokale server (standard til vilkårlig port)                            |

---

### serve

Start en hovedløs Model-of-Design Tools-server til API-adgang. Tjek [server docs](/docs/server) for den fulde HTTP-grænseflade.

```bash
modtools serve
```

Dette starter en HTTP-server, der giver API-adgang til modtools-funktionalitet uden TUI-grænsefladen. Indstil `MOD_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `modtools`).

#### Flag

| Flag         | Beskrivelse                                      |
| ------------ | ------------------------------------------------ |
| `--port`     | Port at lytte på                                 |
| `--hostname` | Værtsnavn at lytte på                            |
| `--mdns`     | Aktiver mDNS-opdagelse                           |
| `--cors`     | Yderligere browseroprindelse til at tillade CORS |

---

### session

Administrator Model-of-Design Tools sessionsholder.

```bash
modtools session [command]
```

---

#### list

Liste over alle Model-of-Design Tools sessioner.

```bash
modtools session list
```

##### Flag

| Flag          | Kort | Beskrivelse                            |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | Begræns til N seneste sessioner        |
| `--format`    |      | Outputformat: tabel eller json (tabel) |

---

### stats

Vis tokenbrug og omkostningsstatistikker for dine Model-of-Design Tools-sessioner.

```bash
modtools stats
```

#### Flag

| Flag        | Beskrivelse                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Vis statistik for de sidste N dage (hele tiden)                             |
| `--tools`   | Antal værktøjer, der skal vises (alle)                                      |
| `--models`  | Vis modelbrugsopdeling (skjult som standard). Send et tal for at vise top N |
| `--project` | Filtre efter projekt (alle projekter, tom streng: nuværende projekt)        |

---

### export

Eksporter sessionsdata som JSON.

```bash
modtools export [sessionID]
```

Hvis du ikke angiver et sessions-id, bliver du bedt om at vælge mellem tilgængelige sessioner.

---

### import

Importer sessionsdata fra en JSON fil eller Model-of-Design Tools del URL.

```bash
modtools import <file>
```

Du kan importere fra en lokal fil eller en Model-of-Design Tools share URL.

```bash
modtools import session.json
modtools import https://opncd.ai/s/abc123
```

---

### web

Start en hovedløs Model-of-Design Tools-server med en webgrænseflade.

```bash
modtools web
```

Dette starter en HTTP-server og åbner en webbrowser for at få adgang til Model-of-Design Tools via en webgrænseflade. Indstil `MOD_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `modtools`).

#### Flag

| Flag         | Beskrivelse                                      |
| ------------ | ------------------------------------------------ |
| `--port`     | Port at lytte på                                 |
| `--hostname` | Værtsnavn at lytte på                            |
| `--mdns`     | Aktiver mDNS-opdagelse                           |
| `--cors`     | Yderligere browseroprindelse til at tillade CORS |

---

### acp

Start en ACP-server (Agent Client Protocol).

```bash
modtools acp
```

Denne kommando starter en ACP-server, der kommunikerer via stdin/stdout ved hjælp af nd-JSON.

#### Flag

| Flag         | Beskrivelse           |
| ------------ | --------------------- |
| `--cwd`      | Arbejdsmappe          |
| `--port`     | Port at lytte på      |
| `--hostname` | Værtsnavn at lytte på |

---

### uninstall

Afinstaller Model-of-Design Tools og fjern alle relaterede filer.

```bash
modtools uninstall
```

#### Flag

| Flag            | Kort | Beskrivelse                                      |
| --------------- | ---- | ------------------------------------------------ |
| `--keep-config` | `-c` | Se konfigurationsfiler                           |
| `--keep-data`   | `-d` | Gem sessionsdata og snapshots                    |
| `--dry-run`     |      | Vis, hvad der ville blive fjernet uden at fjerne |
| `--force`       | `-f` | Spring bekræftelsesspørgsmål over                |

---

### upgrade

Opdaterer Model-of-Design Tools til den nyeste version eller en specifik version.

```bash
modtools upgrade [target]
```

For at opgradere til den nyeste version.

```bash
modtools upgrade
```

For at opgradere til en bestemt version.

```bash
modtools upgrade v0.1.48
```

#### upgrade

| Flag       | Kort | Beskrivelse                                                      |
| ---------- | ---- | ---------------------------------------------------------------- |
| `--method` | `-m` | Installationsmetoden, der blev brugt; curl, npm, pnpm, bun, brew |

---

## Globale flag

modtools CLI tager følgende globale flag.

| Flag           | Kort | Beskrivelse                          |
| -------------- | ---- | ------------------------------------ |
| `--help`       | `-h` | Vis hjælp                            |
| `--version`    | `-v` | Udskriftsversionsnummer              |
| `--print-logs` |      | Udskriv logfiler til stderr          |
| `--log-level`  |      | Logniveau (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

Model-of-Design Tools kan konfigureres ved hjælp af miljøvariabler.

| Variabel                              | Skriv   | Beskrivelse                                                           |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `MOD_AUTO_SHARE`                 | boolean | Del automatisk session                                                |
| `MOD_GIT_BASH_PATH`              | string  | Sti til Git Bash eksekverbar på Windows                               |
| `MOD_CONFIG`                     | string  | Sti til konfigurationsfil                                             |
| `MOD_TUI_CONFIG`                 | string  | Sti til TUI-konfigurationsfil                                         |
| `MOD_CONFIG_DIR`                 | string  | Sti til konfigurationsmappe                                           |
| `MOD_CONFIG_CONTENT`             | string  | Indbygget json-konfigurationsindhold                                  |
| `MOD_DISABLE_AUTOUPDATE`         | boolean | Deaktiver automatisk opdateringskontrol                               |
| `MOD_DISABLE_PRUNE`              | boolean | Deaktiver beskæring af gamle data                                     |
| `MOD_DISABLE_TERMINAL_TITLE`     | boolean | Deaktiver automatisk opdatering af terminaltitel                      |
| `MOD_PERMISSION`                 | string  | Indbygget json-tilladelseskonfiguration                               |
| `MOD_DISABLE_DEFAULT_PLUGINS`    | boolean | Deaktiver standard plugins                                            |
| `MOD_DISABLE_LSP_DOWNLOAD`       | boolean | Deaktiver automatisk LSP-serverdownloads                              |
| `MOD_ENABLE_EXPERIMENTAL_MODELS` | boolean | Aktive eksperimentelle modeller                                       |
| `MOD_DISABLE_AUTOCOMPACT`        | boolean | Deaktiver automatisk kontekstkomprimering                             |
| `MOD_DISABLE_CLAUDE_CODE`        | boolean | Deaktiver læsning fra `.claude` (prompt + færdigheder)                |
| `MOD_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Deaktiver læsning `~/.claude/CLAUDE.md`                               |
| `MOD_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Deaktiver indlæsning af `.claude/skills`                              |
| `MOD_DISABLE_MODELS_FETCH`       | boolean | Deaktivering af modeller fra eksterne kilder                          |
| `MOD_FAKE_VCS`                   | string  | Falsk VCS-udbyder til testformål                                      |
| `MOD_DISABLE_FILETIME_CHECK`     | boolean | Deaktiver filtidskontrol for optimering                               |
| `MOD_CLIENT`                     | string  | Klient-id (standard til `cli`)                                        |
| `MOD_ENABLE_EXA`                 | boolean | Aktiver Exa-websøgeværktøjer                                          |
| `MOD_SERVER_PASSWORD`            | string  | Aktiver grundlæggende godkendelse for `serve`/`web`                   |
| `MOD_SERVER_USERNAME`            | string  | Tilsidesæt grundlæggende godkendelsesbrugernavn (standard `modtools`) |
| `MOD_MODELS_URL`                 | string  | Brugerdefineret URL til hentning af modelkonfiguration                |

---

### Eksperimentel

Disse miljøvariabler muliggør eksperimentelle funktioner, der kan ændres eller fjernes.

| Variabel                                        | Skriv   | Beskrivelse                                |
| ----------------------------------------------- | ------- | ------------------------------------------ |
| `MOD_EXPERIMENTAL`                         | boolean | Aktiver alle eksperimentelle funktioner    |
| `MOD_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Aktiver ikonopdagelse                      |
| `MOD_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Deaktiver kopi ved valg i TUI              |
| `MOD_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | nummer  | Standard timeout for bash-kommandoer i ms  |
| `MOD_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | nummer  | Maks. output-tokens for LLM-svar           |
| `MOD_EXPERIMENTAL_FILEWATCHER`             | boolean | Aktiver filovervågning for hele dir        |
| `MOD_EXPERIMENTAL_OXFMT`                   | boolean | Aktiver oxfmt formatter                    |
| `MOD_EXPERIMENTAL_LSP_TOOL`                | boolean | Aktive eksperimenter LSP værktøj           |
| `MOD_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Deaktiver filovervågning                   |
| `MOD_EXPERIMENTAL_EXA`                     | boolean | Aktive eksperimenter Exa-funktioner        |
| `MOD_EXPERIMENTAL_LSP_TY`                  | boolean | Aktiver TY LSP for python-filer            |
| `MOD_EXPERIMENTAL_MARKDOWN`                | boolean | Aktive eksperimentelle markdown-funktioner |
| `MOD_EXPERIMENTAL_PLAN_MODE`               | boolean | Aktiver plantilstand                       |
